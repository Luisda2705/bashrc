# Automatically continue indents from previous lines
set autoindent
# Can't remember too much, but it makes things even more minimal 
set minibar
# Set wrapping to happen at 60 chars
set fill 60
# Remove the help section from the bottom
set nohelp
# Set the tab size to 4 spaces
set tabsize 4
# Allow nano to be suspended with Ctrl+z
set suspendable
# Syntax highlighting (at least on Arch)
include "/usr/share/nano/*.nanorc"
include "/usr/share/nano-syntax-highlighting/*.nanorc"
